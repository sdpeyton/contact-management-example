// in a production environment config options would be stored
// in a json file and logic will determine which of a series of
// options are selected based on the given environment

export const api_url = 'http://localhost:4000/';